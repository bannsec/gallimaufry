language: python
sudo: enabled
python:
    - 3.5
    - 3.6
before_install:
    - sudo apt-get -qq update
    - sudo apt-get install tshark
install:
    - pip install .[dev]
script:
    - pytest --boxed -n 8 --cov=Gallimaufry tests 
